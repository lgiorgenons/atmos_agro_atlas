# Configuração

Módulos relacionados a `AppConfig` (Pydantic), carregamento de variáveis de ambiente e centralização de parâmetros (paths, tuning, credenciais).

Exemplo esperado:
- `settings.py`: objetos `CoreSettings`, `CatalogSettings`, etc.
- Helpers para injetar configurações nos factories/adapters.
