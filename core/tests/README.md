# Testes

- `unit/`: cobrem estratégias, adapters e serviços individuais (mocks de ports).
- `integration/`: validam o pipeline completo (fixtures de raster/CSV).

Adicionar fixtures reutilizáveis em `tests/fixtures/` assim que começarmos a portar o código.
